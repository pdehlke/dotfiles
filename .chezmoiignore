# Dotfiles
LICENSE
README.md
CHANGELOG.md
doc/
install.sh

# YADR install
Rakefile
bin/

# Docker tests
Dockerfile
docker-compose.yml

# Chrome Custom.css
chrome/

# Vimfiles
vim/

# ZSH
zsh/

{{ if (ne .chezmoi.os "darwin") }}{{/* Don't install MacOS files */}}
# no darwin files
Library/
iTerm2/
{{ end }}

{{ if (ne .chezmoi.os "linux" ) }}{{/* Don't install Linux files */}}
# no linux files
.local/share/fonts/
{{ end }}
