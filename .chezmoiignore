# Dotfiles
LICENSE
README*.md
CHANGELOG.md
docs/
install.sh

# YADR install
Rakefile
bin/*
aliases.sh

# Docker tests
Dockerfile
docker-compose.yml

# Chrome Custom.css
chrome/

# Vimfiles
vim/

# ZSH
zsh/

# Kitty
{{ if not (eq .param.color "solarized" "dracula") }}
.config/kitty/diff.conf
{{ end }}

{{ if (ne .chezmoi.os "darwin") }}{{/* Don't install MacOS files */}}
# no darwin files
Library/
iTerm2/
{{ end }}

{{ if (ne .chezmoi.os "linux" ) }}{{/* Don't install Linux files */}}
# no linux files
.local/share/fonts/
{{ end }}
