# vim:fileencoding=utf-8:ft=conf

[.asdf]
skip = third_party_skipper $MR_ACTION
checkout =
  git clone -- \
      'https://github.com/asdf-vm/asdf.git' '.asdf'
update = ${MR_REPO}/bin/asdf update
post_checkout = ${MR_REPO}/bin/asdf update
post_update = git_last_tag

