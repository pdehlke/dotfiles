source {{ .chezmoi.sourceDir }}/zsh/prezto/runcoms/zshrc

source {{ .chezmoi.sourceDir }}/zsh/prezto-override/theme.zsh

# Don't change. The following determines where YADR is installed.
export yadr={{ .chezmoi.sourceDir }}

for config_file ( {{- .chezmoi.sourceDir -}} /zsh/*.zsh) source $config_file
